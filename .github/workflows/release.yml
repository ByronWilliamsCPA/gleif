# GLEIF Golden Copy CLI - Release Pipeline
#
# Thin caller for the org-level python-release.yml reusable workflow, which
# builds and signs the distribution, generates the SBOM, and publishes the
# GitHub release.

name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: read

jobs:
  release:
    uses: ByronWilliamsCPA/.github/.github/workflows/python-release.yml@987d517d3c8e4b180f4dd15de6d9575f0df91182 # main
    permissions:
      contents: write
      id-token: write
      attestations: write
      issues: write
      pull-requests: write
    with:
      python-version: '3.12'
      source-directory: src
      no-build: false
      semantic-release: false
      publish-to-pypi: false
