# GLEIF Golden Copy CLI - CI Pipeline
#
# Thin caller for the org-level python-ci.yml reusable workflow, which runs
# formatting, linting, type checking, and the test suite.

name: CI

on:
  merge_group:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - develop
      - 'feature/**'
  push:
    branches:
      - main
      - develop

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ByronWilliamsCPA/.github/.github/workflows/python-ci.yml@987d517d3c8e4b180f4dd15de6d9575f0df91182 # main
    permissions:
      contents: read
      checks: write
      pull-requests: write
    with:
      python-version: '3.12'
      source-directory: src
      no-build: false
      coverage-threshold: 80

  ci-gate:
    name: CI Gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [ci]
    if: always()
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - name: Validate CI Results
        run: |
          if [[ "${{ needs.ci.result }}" == "success" ]]; then
            echo "CI Pipeline - All Checks Passed"
          else
            echo "CI Pipeline Failed"
            echo "ci: ${{ needs.ci.result }}"
            exit 1
          fi
